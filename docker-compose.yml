services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: soma-wellness-api

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "127.0.0.1:5000:5000"

    volumes:
      - uploads_data:/app/server/uploads

volumes:
  uploads_data:
