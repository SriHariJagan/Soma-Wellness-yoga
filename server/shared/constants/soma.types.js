// ── SOMA Membership Tiers ──────────────────────────────────
export const SOMA_TIERS = ['JUA', 'AMANI', 'UZIMA', 'FAMILY'];

// ── SOMA Pass Types ────────────────────────────────────────
export const SOMA_PASS_TYPES = ['5_CLASS', '10_CLASS', 'DISCOVERY', 'SINGLE'];

// ── Appointment / Service Types ────────────────────────────
export const APPOINTMENT_STATUSES = ['scheduled', 'completed', 'cancelled', 'no_show'];
export const APPOINTMENT_TYPES = [
  'private_yoga',
  'therapy_assessment',
  'therapy_session',
  'massage',
  'meditation',
  'signature_STILLNESS',
  'signature_ACACIA',
  'signature_FOR_TWO',
  'life_stage',
  'corporate',
  'other',
];

// ── Gift Voucher Statuses ──────────────────────────────────
export const VOUCHER_STATUSES = ['active', 'redeemed', 'expired', 'cancelled'];

// ── Corporate Lead Statuses ────────────────────────────────
export const CORPORATE_LEAD_STATUSES = ['new', 'contacted', 'quoted', 'converted', 'closed', 'rejected'];

// ── SOMA Content Types (for SOMA DAILY) ────────────────────
export const SOMA_CONTENT_TYPES = ['podcast', 'reflection', 'guided_audio', 'reading_note'];
export const SOMA_CONTENT_CADENCE = ['weekly', 'daily', 'monthly', 'seasonal'];

// ── Health Disclosure ──────────────────────────────────────
export const HEALTH_DISCLOSURE_PROMPTS = [
  'pregnancy',
  'recent_surgery',
  'injury',
  'significant_pain',
  'heart_concerns',
  'other',
];

// ── SOMA Reset Components ──────────────────────────────────
export const RESET_COMPONENTS = ['assessment', 'yoga', 'meditation', 'massage', 'homePlan', 'review'];

// ── Founding Settings ──────────────────────────────────────
export const FOUNDING_STATUSES = ['active', 'cap_reached', 'window_expired', 'both_expired'];
